//! Test modules for `free_router`. Split by concern per CLAUDE.md style:
//! fixtures shared across the suite, plus three focused test files.

mod classifier_tests;
mod dispatch_tests;
mod fixtures;
mod pool_tests;
